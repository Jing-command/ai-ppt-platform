# AI 提示词助手功能开发文档

## 一、功能概述

AI 提示词助手是一个智能对话系统，帮助用户优化和生成 PPT 提示词。通过聊天式交互，引导用户明确需求，最终生成专业的 PPT 提示词。

### 核心功能

| 功能 | 描述 |
|------|------|
| 智能对话 | 用户与 AI 实时对话，流式输出响应 |
| 快捷入口 | 提供 4 个预设场景，快速开始对话 |
| 提示词优化 | AI 分析用户描述，生成优化后的提示词 |
| 一键跳转 | 优化后的提示词可直接用于大纲生成 |

---

## 二、页面路由

| 路由 | 页面 | 说明 |
|------|------|------|
| `/tips/prompts` | 提示词助手主页 | 聊天交互界面 |

---

## 三、页面布局

```
┌─────────────────────────────────────────────────────────────────┐
│  导航栏：[返回] AI 提示词助手                                     │
├───────────────────┬─────────────────────────────────────────────┤
│                   │                                             │
│   快捷入口区域     │              聊天区域                        │
│   (约 1/4 宽度)   │              (约 3/4 宽度)                   │
│                   │                                             │
│   ┌─────────────┐ │  ┌───────────────────────────────────────┐ │
│   │ 优化提示词  │ │  │                                       │ │
│   └─────────────┘ │  │  AI: 你好！我是 AI 提示词助手...       │ │
│                   │  │                                       │ │
│   ┌─────────────┐ │  └───────────────────────────────────────┘ │
│   │ 明确主题    │ │                                             │
│   └─────────────┘ │  ┌───────────────────────────────────────┐ │
│                   │  │                                       │ │
│   ┌─────────────┐ │  │  用户: 我想做一个产品发布会PPT         │ │
│   │ 创意灵感    │ │  │                                       │ │
│   └─────────────┘ │  └───────────────────────────────────────┘ │
│                   │                                             │
│   ┌─────────────┐ │  ┌───────────────────────────────────────┐ │
│   │ 受众分析    │ │  │  AI: 很棒的想法！能告诉我更多细节吗？  │ │
│   └─────────────┘ │  │      [优化后的提示词卡片]              │ │
│                   │  └───────────────────────────────────────┘ │
│   ┌─────────────┐ │                                             │
│   │ 💡 使用提示 │ │  ┌───────────────────────────────────────┐ │
│   └─────────────┘ │  │ 输入你的想法或问题...        [发送]   │ │
│                   │  └───────────────────────────────────────┘ │
└───────────────────┴─────────────────────────────────────────────┘
```

---

## 四、技术实现

### 4.1 前端技术栈

| 技术 | 用途 |
|------|------|
| React Hooks | 状态管理 (useState, useEffect, useRef, useCallback) |
| Framer Motion | 动画效果 |
| SSE (Server-Sent Events) | 流式响应处理 |

### 4.2 后端技术栈

| 技术 | 用途 |
|------|------|
| FastAPI | API 框架 |
| StreamingResponse | SSE 流式响应 |
| Pydantic | 数据验证 |

---

## 五、数据结构定义

### 5.1 后端 Schema

```python
# backend/src/ai_ppt/api/v1/schemas/chat.py

class MessageRole(str, Enum):
    """聊天消息角色"""
    USER = "user"
    ASSISTANT = "assistant"
    SYSTEM = "system"

class ChatMessage(BaseModel):
    """聊天消息"""
    role: MessageRole
    content: str

class ChatRequest(BaseModel):
    """聊天请求"""
    messages: List[ChatMessage]
    context: Optional[ChatContext] = None
    stream: bool = True

class ChatResponseChunk(BaseModel):
    """流式响应块"""
    content: str
    isFinished: bool = False
    hasOptimizedPrompt: bool = False
    optimizedPrompt: Optional[str] = None
```

### 5.2 前端类型定义

```typescript
// frontend/lib/api/chat.ts

interface ChatMessage {
    role: 'user' | 'assistant';
    content: string;
}

interface ChatStreamChunk {
    content: string;
    isFinished: boolean;
    hasOptimizedPrompt: boolean;
    optimizedPrompt?: string;
}

interface SendMessageParams {
    messages: ChatMessage[];
    onChunk: (chunk: ChatStreamChunk) => void;
    onError?: (error: Error) => void;
    onComplete?: () => void;
}

// frontend/components/chat/ChatMessage.tsx

interface MessageData {
    id: string;
    role: 'user' | 'assistant';
    content: string;
    isStreaming?: boolean;
    optimizedPrompt?: string;
}

// frontend/components/chat/QuickEntry.tsx

interface QuickEntryItem {
    id: string;
    icon: React.ComponentType<{ className?: string }>;
    title: string;
    description: string;
    prompt: string;
}
```

---

## 六、API 设计

### 6.1 聊天端点

**端点**：`POST /api/v1/chat`

**描述**：发送聊天消息，返回 SSE 流式响应

**请求体**：
```json
{
    "messages": [
        { "role": "user", "content": "我想做一个产品发布会PPT" }
    ]
}
```

**响应**：SSE 流式格式
```
data: {"content": "很棒", "isFinished": false, "hasOptimizedPrompt": false}

data: {"content": "的想法", "isFinished": false, "hasOptimizedPrompt": false}

data: {"content": "！", "isFinished": true, "hasOptimizedPrompt": true, "optimizedPrompt": "主题：产品发布会\n目标受众：媒体和合作伙伴..."}
```

---

## 七、组件结构

```
frontend/
├── app/tips/prompts/
│   └── page.tsx              # 主页面
├── components/chat/
│   ├── index.ts              # 导出索引
│   ├── ChatMessage.tsx       # 消息气泡组件
│   ├── ChatInput.tsx         # 输入框组件
│   ├── PromptCard.tsx        # 优化提示词卡片
│   └── QuickEntry.tsx        # 快捷入口卡片
└── lib/api/
    └── chat.ts               # API 函数
```

---

## 八、组件详解

### 8.1 ChatMessage 消息气泡

**功能**：
- 区分用户和 AI 消息样式
- 支持流式输出时显示闪烁光标
- 自动显示优化后的提示词卡片

**Props**：
```typescript
interface ChatMessageProps {
    message: MessageData;
}
```

**样式**：
- 用户消息：右对齐，蓝色渐变背景
- AI 消息：左对齐，白色背景，带紫色渐变 AI 头像

### 8.2 ChatInput 输入框

**功能**：
- 多行文本输入，自动调整高度（最大 5 行）
- Enter 发送，Shift+Enter 换行
- 发送时禁用输入状态，显示加载动画

**Props**：
```typescript
interface ChatInputProps {
    onSend: (content: string) => void;
    disabled?: boolean;
    placeholder?: string;
}
```

### 8.3 PromptCard 提示词卡片

**功能**：
- 显示优化后的提示词内容
- "复制"按钮：复制到剪贴板
- "使用此提示词"按钮：跳转到 `/outlines/new`

**Props**：
```typescript
interface PromptCardProps {
    prompt: string;
}
```

### 8.4 QuickEntry 快捷入口

**功能**：
- 快捷入口卡片，包含图标、标题、描述
- 悬停动画：缩放、上移、背景渐变
- 点击时触发回调发送预设提示词

**Props**：
```typescript
interface QuickEntryProps {
    item: QuickEntryItem;
    onClick: (prompt: string) => void;
    index: number;
}
```

---

## 九、快捷入口配置

```typescript
const quickEntries: QuickEntryItem[] = [
    {
        id: 'optimize',
        icon: Wand2,
        title: '优化提示词',
        description: '让 AI 帮你优化现有的 PPT 提示词，使其更加清晰、专业',
        prompt: '请帮我优化一个 PPT 提示词，让生成的效果更好。'
    },
    {
        id: 'topic',
        icon: Target,
        title: '明确主题',
        description: '不确定 PPT 主题？AI 帮你梳理思路，确定核心内容',
        prompt: '我想做一个 PPT，但还不确定具体主题，能帮我梳理一下思路吗？'
    },
    {
        id: 'creative',
        icon: Lightbulb,
        title: '创意灵感',
        description: '获取 PPT 创意灵感和设计建议，让你的演示更出彩',
        prompt: '我需要一些 PPT 创意灵感，能让我的演示更加出彩吗？'
    },
    {
        id: 'audience',
        icon: Users,
        title: '受众分析',
        description: '分析目标受众，定制最适合的内容风格和表达方式',
        prompt: '请帮我分析 PPT 的目标受众，并给出内容风格建议。'
    }
];
```

---

## 十、用户流程

### 10.1 基本对话流程

```
用户进入 /tips/prompts
    ↓
AI 自动发送欢迎消息
    ↓
用户输入问题或点击快捷入口
    ↓
消息显示在聊天区域
    ↓
AI 流式输出响应（打字机效果）
    ↓
[可选] 显示优化后的提示词卡片
    ↓
用户点击"使用此提示词"
    ↓
跳转到 /outlines/new 并自动填入
```

### 10.2 流式响应处理流程

```
前端发送 POST /api/v1/chat
    ↓
后端返回 SSE 流
    ↓
前端 ReadableStream 读取数据
    ↓
按行解析 SSE 格式 (data: {...})
    ↓
解析 JSON 获取 chunk
    ↓
更新消息内容 (追加 content)
    ↓
isFinished=true 时结束
```

---

## 十一、后端服务逻辑

### 11.1 意图分析

```python
class IntentType(str, Enum):
    CLARIFICATION = "clarification"      # 需要澄清
    PROMPT_OPTIMIZATION = "prompt_optimization"  # 提示词优化
    SUGGESTION = "suggestion"            # 提供建议
    GENERAL = "general"                  # 一般对话
```

### 11.2 响应生成逻辑

1. **分析用户消息**：识别意图类型
2. **判断信息完整性**：
   - 缺少信息 → 追问用户
   - 信息足够 → 生成优化提示词
3. **生成流式响应**：逐字输出
4. **附加优化提示词**：在响应末尾附加 `optimizedPrompt`

---

## 十二、注意事项

### 12.1 性能优化

- 使用 `useCallback` 缓存回调函数
- 消息列表使用 `AnimatePresence` 优化动画
- 流式响应使用缓冲区处理不完整数据

### 12.2 错误处理

- 网络错误：显示友好提示
- JSON 解析错误：跳过当前 chunk
- 认证失败：跳转登录页

### 12.3 用户体验

- 自动滚动到最新消息
- 流式输出时显示闪烁光标
- 输入框自动聚焦

---

## 十三、文件清单

| 文件路径 | 说明 |
|----------|------|
| `frontend/app/tips/prompts/page.tsx` | 主页面 |
| `frontend/components/chat/index.ts` | 组件导出 |
| `frontend/components/chat/ChatMessage.tsx` | 消息气泡 |
| `frontend/components/chat/ChatInput.tsx` | 输入框 |
| `frontend/components/chat/PromptCard.tsx` | 提示词卡片 |
| `frontend/components/chat/QuickEntry.tsx` | 快捷入口 |
| `frontend/lib/api/chat.ts` | API 函数 |
| `backend/src/ai_ppt/api/v1/endpoints/chat.py` | API 端点 |
| `backend/src/ai_ppt/api/v1/schemas/chat.py` | Schema 定义 |
| `backend/src/ai_ppt/services/chat_service.py` | 服务逻辑 |
