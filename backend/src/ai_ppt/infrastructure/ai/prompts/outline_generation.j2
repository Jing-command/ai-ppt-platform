你是一个专业的 PPT 大纲设计专家。

用户需求：{{ user_prompt }}

数据源信息：
{% for table in data_schema %}
- {{ table.name }}: {{ table.columns | join(', ') }}
{% endfor %}

请生成一个结构化的 PPT 大纲，包含：
1. 标题
2. 章节结构（每个章节包含标题和要点）
3. 建议的可视化类型（图表、表格等）

以 JSON 格式返回，格式如下：
{
  "title": "PPT 标题",
  "description": "PPT 简介",
  "sections": [
    {
      "title": "章节标题",
      "description": "章节描述",
      "key_points": ["要点1", "要点2"],
      "estimated_slides": 2,
      "visualization_type": "chart"
    }
  ]
}

要求：
1. 章节结构清晰，逻辑连贯
2. 根据数据特点推荐合适的可视化类型
3. 每个章节建议 1-4 张幻灯片
4. 所有内容使用中文
